<script>
  const btnMostrarFormularios = document.querySelectorAll('.edit_form')
  const formEditarSesion = document.getElementById('form-editar-sesion')
  const btnCerrarFormulario = document.getElementById('btnCerrarFormulario')

  const btnAgregarEvaluacion = document.getElementById('btnAgregarEvaluacion')
  const formAgregarEvaluacion = document.getElementById('form-agregar-evaluacion')
  const btnCerrarFormularioAgr = document.getElementById('btnCerrarFormAgregar')
  
  // Agregar evento de clic a todos los botones
btnMostrarFormularios.forEach(function(btn) {
    btn.addEventListener('click', function() {
        
        const dataId = btn.dataset.id
        dataIdInput.value = dataId
        formEditarSesion.style.display = 'block' // Mostrar el formulario al hacer clic en cualquier botón
        console.log(dataId)
        window.scrollTo({
          top: 0,
        });
        window.onscroll = function() {
          window.scrollTo(0, 0);
        };

    });
});

btnAgregarEvaluacion.addEventListener('click', function(){
    formAgregarEvaluacion.style.display = 'block' // Mostrar el formulario al hacer clic en cualquier botón
        window.scrollTo({
          top: 0,
        });
        window.onscroll = function() {
          window.scrollTo(0, 0);
        };
});

function desbloquearScroll() {
  window.onscroll = null;
}

btnCerrarFormulario.addEventListener('click', function() {
    formEditarSesion.style.display = 'none'; // Ocultar el formulario al hacer clic en el botón de cerrar
    desbloquearScroll()
});
btnCerrarFormAgregar.addEventListener('click', function(){
    formAgregarEvaluacion.style.display = 'none';
    desbloquearScroll()
})

</script>